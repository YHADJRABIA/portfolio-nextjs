import React, { FC } from "react";
import Hero from "./Hero";
import Nav from "./Nav";

const Header: FC = () => {
  return (
    <header>
      <Hero />
    </header>
  );
};

export default Header;
